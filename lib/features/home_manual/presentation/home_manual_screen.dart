import 'package:flutter/material.dart';

class HomeManualScreen extends StatelessWidget {
  const HomeManualScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Home Manual')));
  }
}
